import {DeleteButtonPipe} from './delete-button.pipe';

describe('DeleteButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new DeleteButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
